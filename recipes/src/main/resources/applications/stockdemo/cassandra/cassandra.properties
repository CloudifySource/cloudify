version = "0.8.7"
name = "cassandra"
zipName = "apache-${name}-${version}-bin.tar.gz"
installDir = System.properties["user.home"] + "/.cloudify"
home = "${installDir}/apache-${name}-${version}"
downloadPath = "http://repository.cloudifysource.org/org/apache/${name}/${version}/${zipName}"
script = "${home}/bin/${name}"
